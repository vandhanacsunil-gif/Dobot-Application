To run this application, the backend and frontend need to be started separately

Backend: Go to the backend folder and execute "npm start"  to start the server.

Frontend: Go to the frontend folder and execute "npm run dev" to start the client application.
